# Server-side only (no VITE_ prefix -> never bundled into the client).
# Read by server/index.js to power POST /api/generate and /api/revise
# during `npm run dev` / `npm run preview`.
# Without it, the app runs fully offline using local fallback data.
GEMINI_API_KEY=
